# Dependencies
node_modules

# TypeScript cache
.tsbuildinfo

# Compilation Artifacts
# Ignore files generated by the TypeScript compiler (tsc)
# It is common practice to output these to a specific directory (e.g., 'dist' or 'build')
dist/
build/
*.js
*.js.map

# Optional: if you use a 'src' folder for source files and want to ignore JS files
# generated alongside the source TS files (less common but an option)
# src/*.js
# src/*.js.map

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
lerna-debug.log*

# Runtime data
pids
*.pid
*.seed

# IDE and OS Files (Optional, but recommended)
.vscode/
.idea/
.DS_Store
Thumbs.db
